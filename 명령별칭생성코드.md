# Git 별칭(Alias) 설정



```bash
git config --global alias.ss "status -s"
# status -s를 ss로 별칭을 지정한다.

git config --global alias.s "status"
# status를 s로 별칭을 지정한다.

git config --global alias.co "checkout"
# checkout을 co로 별칭을 지정한다.

git config --global alias.c "commit"
# commit을 c로 별칭을 지정한다.
